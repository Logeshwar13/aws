# AWS Cloud Club - Flutter + Supabase

## Setup
1. Create Supabase project, copy Project URL & anon key.
2. Run SQL to create `events` and `admins` tables and storage buckets.
3. Update `lib/config/supabase_config.dart` with your keys.
4. Add initial admin user and insert into `admins` table.
5. Run:
